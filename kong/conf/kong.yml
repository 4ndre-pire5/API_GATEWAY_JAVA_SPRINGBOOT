_format_version: "3.0"
_transform: true

services:
  - name: OrdemServico
    protocol: http
    host: localhost
    port: 8080
    path: /api/v1/os
    routes:
      - name: OrdemServico
        methods:
          - GET
            POST
            PATCH
            DELETE
        paths:
          - /v1/gateway/os
        strip_path: true
